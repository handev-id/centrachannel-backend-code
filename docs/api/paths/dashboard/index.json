{
  "get": {
    "summary": "Get comprehensive dashboard analytics with summary, conversations breakdown, contacts, messages, campaigns, and agent workload",
    "tags": ["Dashboard"],
    "security": [{ "bearerAuth": [] }],
    "parameters": [
      {
        "name": "days",
        "in": "query",
        "required": false,
        "schema": { "type": "integer", "minimum": 1, "maximum": 365, "default": 30 },
        "description": "Rolling time window in days for \"new contacts\", \"new conversations\", trends, and message history"
      }
    ],
    "responses": {
      "200": {
        "description": "Dashboard analytics",
        "content": {
          "application/json": {
            "schema": { "$ref": "../../components/schemas/DashboardStats.json" }
          }
        }
      },
      "401": {
        "description": "Unauthorized"
      },
      "422": {
        "description": "Validation error"
      }
    }
  }
}
